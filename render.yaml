services:
  - type: web
    name: intraday-options-bot
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: USE_ALPACA_PAPER_EXECUTION
        value: "true"
